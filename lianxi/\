#include <stdio.h>
#include <string.h>

int main(int argc,char *argv[])
{
	char str[100] = "hello;akaedu;world";
	char *p;
	printf("%s\n",strtok(str,";"));
	while(p = strtok(NULL,";"))
	printf("%s\n",p);
}
