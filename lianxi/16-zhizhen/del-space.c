#include <stdio.h>

void del_space(char *buf)
{
	char *p,*q;
	p = q = buf;
	int flag = 0;
	while
	

	
}

int main(void)
{
	char buf[64];

	fgets(buf,sizeof(buf),stdin);
	del_space(buf);

	return 0;
}
